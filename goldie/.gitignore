node_modules/
# out/ is working state: the cut screens and the manifests carry absolute paths
# of this machine, and the framed strips are published to ../2.x/, which is the
# committed artwork. Nothing here is the set.
out/
